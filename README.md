# Linux-Scripts
It consists in two scripts:
 One, debownload.sh, consists in creating a website using Apache, where people can download .deb files -from my own computer by clicking on links.
 The second one, quota.sh, verifies that every user and group respect their soft and hard disk limits, creates limits and sends mails to users when grace time is up.
